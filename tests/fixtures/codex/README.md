Fixtures use invented IDs, paths, model names, plan text and results. Current shapes mirror the 20-file local 0.154.0 survey; plan, parent, compaction, exec/MCP and legacy compatibility records are synthetic, not proof of observed runtime support. partial.jsonl deliberately ends mid-record. No account identity was observed; log quota fallback is disabled. Protocol fixture contains the exact exported selected types.
